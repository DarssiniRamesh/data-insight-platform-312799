# Cypress (E2E) - Scaffold Placeholder

This folder is a placeholder for future E2E setup per the React V2 plan.

Suggested future steps:
1) Add Cypress dependency.
2) Add `cypress.config.js`
3) Add E2E specs for negative scenarios:
   - 422 FreshnessCheckFailed
   - 422 GateComplianceFailed (schema references)
   - 403 PolicyDenied
   - 422 publish blocked without approval

For now, use React Testing Library tests under `src/**.test.js`.
